import { PageHeader } from '../../components/shared/PageHeader';

export function ExpenseListPage() {
  return (
    <div>
      <PageHeader title="Expenses" description="Expense table, filters, and category breakdown hooks belong here." />
    </div>
  );
}
