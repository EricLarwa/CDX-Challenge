import { PageHeader } from '../../components/shared/PageHeader';

export function InvoiceListPage() {
  return (
    <div>
      <PageHeader
        eyebrow="Invoicing"
        title="Invoices"
        description="Ready for TanStack Table, filters, status badges, and quick actions."
      />
    </div>
  );
}
