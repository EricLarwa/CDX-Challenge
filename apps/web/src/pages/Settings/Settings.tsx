import { PageHeader } from '../../components/shared/PageHeader';

export function SettingsPage() {
  return (
    <div>
      <PageHeader title="Settings" description="Business defaults, currency, and invoice defaults will live here." />
    </div>
  );
}
